import { VideoBackgroundOptions } from "./video-background-options";

export interface VideoBackgroundSimulationOptions extends VideoBackgroundOptions {
    x: number
    y: number
    width: number
    height: number
}