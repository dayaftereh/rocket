export interface VideoBackgroundOptions {
    color: string
    visible: boolean

    speedMinimum: number
    speedMaximum: number
    radiusMinimum: number
    radiusMaximum: number

    distance: number

    particles: number
}